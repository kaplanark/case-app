import React from 'react';
import { View } from 'react-native';
import { NavigationAction } from '@react-navigation/native';

export function HomeScreen({ navigation }:{navigation:NavigationAction}):React.ReactElement {
  return (
    <View>
      
    </View>
  );
}